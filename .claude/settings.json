{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "$root = git rev-parse --show-toplevel 2>$null; if ($root) { $s = Join-Path $root '.claude/hooks/load-agent-guide.ps1'; if (Test-Path $s) { pwsh -NoProfile -ExecutionPolicy Bypass -File $s } }",
            "timeout": 15,
            "statusMessage": "Loading AGENT_GUIDE.md"
          }
        ]
      }
    ]
  }
}
